# Runtime dependencies.
#
# Compatible-release pins (~=) rather than exact ones: a rebuild picks up patch
# releases, which is what keeps the image scan passing months after this was
# written. Run `pip-compile` into a requirements.lock and install from that
# instead if byte-identical rebuilds matter more to you than staying patched.
fastapi~=0.115
uvicorn[standard]~=0.32
# [binary] ships prebuilt libpq wheels, so the image needs no compiler and no
# libpq-dev; [pool] provides the connection pool used in app/db.py.
psycopg[binary,pool]~=3.2
